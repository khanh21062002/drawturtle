#include<bits/stdc++.h>
#include<iostream>

using namespace std;

int main(){
    // freopen("bignumber.inp", "r", stdin);
    // freopen("bignumber.out", "w", stdout);
    int t ;
    freopen("bignumber.inp", "r", stdin);
    cin >> t;
    while (t--)
    {
        long long a,b;
        freopen("bignumber.inp", "r", stdin);
        cin >> a >> b;
        cout << a + b << endl;
        freopen("bignumber.out", "w", stdout);
        cout << a * b << endl;
        freopen("bignumber.out", "w", stdout);
    }
}